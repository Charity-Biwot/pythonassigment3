subject = {"physics":23, "math":24,"chem":32}
subject["math"]
##accessing all dict by looping
for subjects in subject:
    print("subjects",":",subject[subjects])
##accessing dict keys and values
    subject.keys()
    dict_keys = (["physics","math","chem"])
    subject.values()
    dict_values = ([23,24,32])
    ##converting into list
    list(subject.keys())
