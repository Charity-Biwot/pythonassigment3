from audioop import reverse


def palendrome(reverse_name):
    reverse.name=reverse_name[::-1]
    if name == reverse_name:
        print(f"{name} is a palendrome")
    else:
        print(f"{name} is not palendrome")

        
            
